# Generated release artifacts

This directory is intentionally empty in source control. `build_release.ps1`
writes signed or unsigned installers, ZIPs, optional portable executables, and
`SHA256SUMS.txt` here.
